"""
Automate the process of transfering the files from an external source
to the companies internal network
"""

import ftplib
import os
import shutil
import sched

# Set up the log file to undrstand the files that were tranferred success

# Get the connection to the external network

# Iterate trhough the files and download them into the local

# Move the files from the local directory to the internal network

# Schedule the script to run everyday